 npm create vite@latest <nome> --template react
 cd nome
 npm install
 nvm install node switch alla versione 
 per runnare = npm run dev
per vedere le porte che utilizza il nostro pc = less /etc/services



ripassare UserEffect 
ripassare i childrean

ripassare chiamate rest

 
installare l'ultima versione di route (singolo progetto) = npm install react-router-dom@latest